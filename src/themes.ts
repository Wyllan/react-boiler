import { DefaultTheme } from 'styled-components'

// buildable theme type based on styled.d.ts

export const customTheme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    primary: 'white',
    secondary: 'black',
    tertiary: '#03cff8',
  },
}
